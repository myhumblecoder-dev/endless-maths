# Copy to .env.local (or run: pnpm dlx vercel env pull .env.local)
# Never commit real values.

# Public URL of the deployed site. Used for absolute URLs and metadata.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Server-only. API key for the generative-AI provider that produces problems.
# Not yet wired up — see README "Notes".
# AI_API_KEY=
